# 🎓 从零开发Meme代币合约 - 完整学习路线

## 📌 你的现状

✅ **已理解**：代币的基本概念  
❓ **困惑点**：不知道如何将概念转化为代码  
🎯 **目标**：独立开发出SHIB风格的Meme代币合约

---

## 🗺️ 完整学习路线（分7天完成）

### 📅 **第1天：理论基础 + 环境搭建**

#### 上午：理论学习（3小时）

**任务1：理解核心概念**
```
1. 什么是ERC20代币？
   → 阅读：https://eips.ethereum.org/EIPS/eip-20
   → 理解：transfer、approve、transferFrom三个核心函数
   
2. 什么是Uniswap和流动性池？
   → 观看：Uniswap V2工作原理视频
   → 理解：x * y = k 恒定乘积公式
   
3. 什么是代币税？
   → 阅读本项目的：docs/理论知识分析.md
   → 重点理解：税费如何在转账时扣除和分配
```

**检查点：**
- [ ] 能用自己的话解释什么是ERC20
- [ ] 能画出流动性池的工作流程图
- [ ] 能说出代币税的3种常见用途

#### 下午：环境搭建（2小时）

**任务2：安装开发工具**

```bash
# 1. 安装Node.js (版本 >= 16)
# 下载地址：https://nodejs.org/

# 2. 验证安装
node --version
npm --version

# 3. 创建项目目录
mkdir my-meme-token
cd my-meme-token

# 4. 初始化项目
npm init -y

# 5. 安装Hardhat
npm install --save-dev hardhat

# 6. 初始化Hardhat项目
npx hardhat init
# 选择：Create a JavaScript project
```

**任务3：安装依赖**

```bash
npm install @openzeppelin/contracts
npm install --save-dev @nomicfoundation/hardhat-toolbox
```

**检查点：**
- [ ] 能成功运行 `npx hardhat compile`
- [ ] 能成功运行 `npx hardhat test`
- [ ] 理解项目目录结构

---

### 📅 **第2天：从最简单的代币开始**

#### 上午：写第一个ERC20代币（3小时）

**Step 1：理解OpenZeppelin的ERC20**

先看看最简单的代币长什么样：

```solidity
// contracts/MyFirstToken.sol
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

import "@openzeppelin/contracts/token/ERC20/ERC20.sol";

contract MyFirstToken is ERC20 {
    constructor() ERC20("My First Token", "MFT") {
        _mint(msg.sender, 1000000 * 10**18);
    }
}
```

**动手做：**
```bash
# 1. 创建文件 contracts/MyFirstToken.sol
# 2. 复制上面的代码
# 3. 编译
npx hardhat compile

# 4. 写个测试
```

**测试代码：**
```javascript
// test/MyFirstToken.test.js
const { expect } = require("chai");

describe("MyFirstToken", function () {
    it("应该铸造100万代币给部署者", async function () {
        const [owner] = await ethers.getSigners();
        
        const Token = await ethers.getContractFactory("MyFirstToken");
        const token = await Token.deploy();
        
        const balance = await token.balanceOf(owner.address);
        expect(balance).to.equal(ethers.parseEther("1000000"));
    });
});
```

**检查点：**
- [ ] 能编译通过
- [ ] 能测试通过
- [ ] 理解 ERC20、constructor、_mint 的作用

#### 下午：添加Ownable权限（2小时）

**Step 2：添加Owner权限控制**

```solidity
// contracts/MySecondToken.sol
pragma solidity ^0.8.20;

import "@openzeppelin/contracts/token/ERC20/ERC20.sol";
import "@openzeppelin/contracts/access/Ownable.sol";

contract MySecondToken is ERC20, Ownable {
    constructor() ERC20("My Second Token", "MST") Ownable(msg.sender) {
        _mint(msg.sender, 1000000 * 10**18);
    }
    
    // 只有owner能铸造新代币
    function mint(address to, uint256 amount) external onlyOwner {
        _mint(to, amount);
    }
}
```

**动手做：**
1. 创建新合约文件
2. 写测试验证只有owner能mint
3. 运行测试

**检查点：**
- [ ] 理解 Ownable 的作用
- [ ] 理解 onlyOwner 修饰器
- [ ] 能写测试验证权限

---

### 📅 **第3天：理解转账逻辑**

#### 核心目标：理解 _update 函数

**Step 3：阅读OpenZeppelin源码**

```bash
# 找到这个文件阅读
node_modules/@openzeppelin/contracts/token/ERC20/ERC20.sol
```

**重点理解：**
```solidity
function _update(address from, address to, uint256 value) internal virtual {
    // 这个函数在每次转账时都会被调用！
    // from: 发送者
    // to: 接收者
    // value: 金额
}
```

**动手做：重写 _update 添加日志**

```solidity
// contracts/MyThirdToken.sol
pragma solidity ^0.8.20;

import "@openzeppelin/contracts/token/ERC20/ERC20.sol";

contract MyThirdToken is ERC20 {
    event TransferLog(address from, address to, uint256 amount, uint);
    
    constructor() ERC20("My Third Token", "MTT") {
        _mint(msg.sender, 1000000 * 10**18);
    }
    
    // 重写 _update 函数
    function _update(address from, address to, uint256 value) internal override {
        // 先执行原有逻辑
        super._update(from, to, value);
        
        // 添加自己的逻辑：记录日志
        emit TransferLog(from, to, value);
    }
}
```

**检查点：**
- [ ] 理解每次转账都会调用 _update
- [ ] 理解 override 和 super 的用法
- [ ] 能在 _update 中添加自定义逻辑

---

### 📅 **第4天：实现代币税功能**

#### 上午：最简单的税费逻辑（3小时）

**Step 4：固定10%税费**

```solidity
// contracts/TaxToken.sol
pragma solidity ^0.8.20;

import "@openzeppelin/contracts/token/ERC20/ERC20.sol";
import "@openzeppelin/contracts/access/Ownable.sol";

contract TaxToken is ERC20, Ownable {
    uint256 public constant TAX_RATE = 1000; // 10% (基点)
    uint256 public constant BASIS_POINTS = 10000; // 100%
    
    address public taxWallet;
    
    constructor(address _taxWallet) ERC20("Tax Token", "TAX") Ownable(msg.sender) {
        taxWallet = _taxWallet;
        _mint(msg.sender, 1000000 * 10**18);
    }
    
    function _update(address from, address to, uint256 amount) internal override {
        // 铸造和销毁不收税
        if (from == address(0) || to == address(0)) {
            super._update(from, to, amount);
            return;
        }
        
        // Owner转账不收税
        if (from == owner() || to == owner()) {
            super._update(from, to, amount);
            return;
        }
        
        // 计算税费
        uint256 taxAmount = (amount * TAX_RATE) / BASIS_POINTS;
        uint256 transferAmount = amount - taxAmount;
        
        // 先转税费给taxWallet
        if (taxAmount > 0) {
            super._update(from, taxWallet, taxAmount);
        }
        
        // 再转剩余金额给接收者
        super._update(from, to, transferAmount);
    }
}
```

**测试代码：**
```javascript
it("应该收取10%税费", async function () {
    const [owner, taxWallet, user1, user2] = await ethers.getSigners();
    
    const Token = await ethers.getContractFactory("TaxToken");
    const token = await Token.deploy(taxWallet.address);
    
    // Owner转100给user1
    await token.transfer(user1.address, ethers.parseEther("100"));
    
    // user1转100给user2（应该收10%税费）
    await token.connect(user1).transfer(user2.address, ethers.parseEther("100"));
    
    // user2应该收到90
    const user2Balance = await token.balanceOf(user2.address);
    expect(user2Balance).to.equal(ethers.parseEther("90"));
    
    // taxWallet应该收到10
    const taxBalance = await token.balanceOf(taxWallet.address);
    expect(taxBalance).to.equal(ethers.parseEther("10"));
});
```

**检查点：**
- [ ] 能实现固定税率
- [ ] 能正确计算税费
- [ ] 能测试验证税费收取

#### 下午：可配置的税率（2小时）

**Step 5：买入税 vs 卖出税**

```solidity
contract TaxTokenV2 is ERC20, Ownable {
    uint256 public buyTaxRate = 500;   // 5%
    uint256 public sellTaxRate = 1000; // 10%
    
    address public uniswapPair; // 交易对地址
    
    // 设置税率
    function setTaxRates(uint256 _buyTax, uint256 _sellTax) external onlyOwner {
        require(_buyTax <= 2500, "Buy tax too high"); // 最高25%
        require(_sellTax <= 2500, "Sell tax too high");
        buyTaxRate = _buyTax;
        sellTaxRate = _sellTax;
    }
    
    function _update(address from, address to, uint256 amount) internal override {
        // ... 前面的检查 ...
        
        uint256 taxRate;
        
        // 判断是买入还是卖出
        if (from == uniswapPair) {
            taxRate = buyTaxRate; // 从池子买入
        } else if (to == uniswapPair) {
            taxRate = sellTaxRate; // 卖到池子
        } else {
            taxRate = 0; // 钱包间转账不收税
        }
        
        // ... 计算和转账 ...
    }
}
```

**检查点：**
- [ ] 理解买入税和卖出税的区别
- [ ] 能判断交易方向
- [ ] 能动态调整税率

---

### 📅 **第5天：实现交易限制**

#### 上午：单笔限额和最大持有（3小时）

**Step 6：添加交易限制**

```solidity
contract LimitedToken is ERC20, Ownable {
    uint256 public maxTxAmount;
    uint256 public maxWalletAmount;
    bool public limitsEnabled = true;
    
    mapping(address => bool) public isExcludedFromLimits;
    
    constructor() ERC20("Limited Token", "LMT") Ownable(msg.sender) {
        uint256 totalSupply = 1000000 * 10**18;
        _mint(msg.sender, totalSupply);
        
        // 设置限制为总供应量的百分比
        maxTxAmount = totalSupply * 5 / 1000;      // 0.5%
        maxWalletAmount = totalSupply * 20 / 1000; // 2%
        
        // Owner豁免
        isExcludedFromLimits[msg.sender] = true;
    }
    
    function _update(address from, address to, uint256 amount) internal override {
        // 铸造和销毁不限制
        if (from == address(0) || to == address(0)) {
            super._update(from, to, amount);
            return;
        }
        
        // 检查限制
        if (limitsEnabled && !isExcludedFromLimits[from] && !isExcludedFromLimits[to]) {
            // 检查单笔限额
            require(amount <= maxTxAmount, "超过单笔限额");
            
            // 检查接收者持有量
            require(
                balanceOf(to) + amount <= maxWalletAmount,
                "超过最大持有量"
            );
        }
        
        super._update(from, to, amount);
    }
}
```

**检查点：**
- [ ] 理解为什么需要限制
- [ ] 能实现单笔和持有限额
- [ ] 能设置豁免地址

#### 下午：冷却期机制（2小时）

**Step 7：防止高频交易**

```solidity
contract CooldownToken is ERC20 {
    uint256 public cooldownPeriod = 60; // 60秒
    mapping(address => uint256) private _lastTransferTime;
    
    function _update(address from, address to, uint256 amount) internal override {
        // ... 其他检查 ...
        
        // 检查冷却期（卖出时）
        if (from != address(0) && to != address(0)) {
            require(
                block.timestamp >= _lastTransferTime[from] + cooldownPeriod,
                "冷却期未结束"
            );
            _lastTransferTime[from] = block.timestamp;
        }
        
        super._update(from, to, amount);
    }
}
```

**检查点：**
- [ ] 理解 block.timestamp 的使用
- [ ] 能实现时间限制
- [ ] 能测试冷却期逻辑

---

### 📅 **第6天：集成Uniswap流动性池**

#### 关键概念：自动添加流动性

**Step 8：理解流动性池交互**

```solidity
// 1. 首先理解Uniswap接口
interface IUniswapV2Router02 {
    function swapExactTokensForETHSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
    
    function addLiquidityETH(
        address token,
        uint amountTokenDesired,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external payable returns (uint amountToken, uint amountETH, uint liquidity);
}
```

**Step 9：实现自动swap和添加流动性**

```solidity
contract AutoLiquidityToken is ERC20, Ownable {
    IUniswapV2Router02 public uniswapRouter;
    address public uniswapPair;
    
    uint256 public swapThreshold; // 累积多少代币后触发swap
    uint256 private _pendingTaxTokens; // 累积的税费代币
    
    bool private _inSwap; // 防重入标志
    
    modifier lockTheSwap {
        _inSwap = true;
        _;
        _inSwap = false;
    }
    
    function _update(address from, address to, uint256 amount) internal override {
        // ... 计算税费 ...
        
        // 累积税费
        if (taxAmount > 0) {
            _pendingTaxTokens += taxAmount;
            super._update(from, address(this), taxAmount);
        }
        
        // 检查是否需要swap
        bool shouldSwap = !_inSwap && 
                          to == uniswapPair && 
                          _pendingTaxTokens >= swapThreshold;
        
        if (shouldSwap) {
            _swapAndAddLiquidity();
        }
        
        // 转账
        super._update(from, to, transferAmount);
    }
    
    function _swapAndAddLiquidity() private lockTheSwap {
        uint256 tokensToSwap = _pendingTaxTokens;
        _pendingTaxTokens = 0;
        
        // 一半swap成ETH
        uint256 half = tokensToSwap / 2;
        uint256 otherHalf = tokensToSwap - half;
        
        // Swap代币为ETH
        _swapTokensForETH(half);
        
        // 用ETH+代币添加流动性
        uint256 ethBalance = address(this).balance;
        _addLiquidity(otherHalf, ethBalance);
    }
    
    function _swapTokensForETH(uint256 tokenAmount) private {
        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = uniswapRouter.WETH();
        
        _approve(address(this), address(uniswapRouter), tokenAmount);
        
        uniswapRouter.swapExactTokensForETHSupportingFeeOnTransferTokens(
            tokenAmount,
            0,
            path,
            address(this),
            block.timestamp
        );
    }
}
```

**检查点：**
- [ ] 理解为什么需要防重入
- [ ] 理解swap的流程
- [ ] 理解如何添加流动性

---

### 📅 **第7天：整合所有功能**

#### 上午：组合所有功能（3小时）

**Step 10：创建完整合约**

现在把所有功能整合到一起：

```solidity
contract ShibaMemeToken is ERC20, Ownable, ReentrancyGuard {
    // 1. 税费相关
    uint256 public buyTaxRate;
    uint256 public sellTaxRate;
    
    // 2. 限制相关
    uint256 public maxTxAmount;
    uint256 public maxWalletAmount;
    uint256 public cooldownPeriod;
    
    // 3. Uniswap相关
    IUniswapV2Router02 public uniswapRouter;
    address public uniswapPair;
    
    // 4. 映射和状态
    mapping(address => bool) public isExcludedFromFees;
    mapping(address => bool) public isBlacklisted;
    mapping(address => uint256) private _lastTransferTime;
    
    // ... 实现所有功能 ...
}
```

**检查点：**
- [ ] 所有功能都能正常工作
- [ ] 功能之间不冲突
- [ ] 测试全部通过

#### 下午：编写测试和文档（2小时）

**Step 11：完善测试**

```javascript
describe("完整功能测试", function () {
    it("税费 + 限制 + 流动性应该协同工作", async function () {
        // 1. 部署合约
        // 2. 添加流动性
        // 3. 启用交易
        // 4. 测试买入（验证税费）
        // 5. 测试卖出（验证限制和冷却期）
        // 6. 验证自动流动性添加
    });
});
```

**检查点：**
- [ ] 测试覆盖所有功能
- [ ] 边界条件测试
- [ ] 写好README文档

---

## 🎯 学习检查清单

完成每个阶段后，确认你能回答这些问题：

### 第1-2天
- [ ] ERC20的核心函数是什么？
- [ ] 什么是 constructor？
- [ ] 什么是 modifier？
- [ ] OpenZeppelin是什么？

### 第3-4天
- [ ] _update 函数什么时候被调用？
- [ ] super 关键字的作用？
- [ ] 如何在转账时扣除税费？
- [ ] 基点（basis points）是什么？

### 第5-6天
- [ ] 如何限制单笔交易金额？
- [ ] mapping 的作用是什么？
- [ ] block.timestamp 是什么？
- [ ] 什么是重入攻击？

### 第7天
- [ ] 如何整合多个功能？
- [ ] 如何处理功能冲突？
- [ ] 如何写全面的测试？

---

## 💡 关键学习技巧

### 1. 不要一次性看完所有代码
**错误做法：**
❌ 直接看完整的 ShibaMemeToken.sol (635行)
❌ 试图理解所有功能

**正确做法：**
✅ 从10行代码开始
✅ 每天增加一个小功能
✅ 理解一个概念再继续

### 2. 多写测试
**每写一个功能，立即写测试：**
```javascript
// 写了税费功能？
it("应该收取正确的税费", async function () {
    // 测试代码
});

// 写了限制功能？
it("应该阻止超额交易", async function () {
    // 测试代码
});
```

### 3. 画图理解流程
**建议画的图：**
1. 转账流程图
2. 税费分配流程
3. swap和添加流动性流程

### 4. 阅读OpenZeppelin源码
**必读文件：**
```
node_modules/@openzeppelin/contracts/token/ERC20/ERC20.sol
node_modules/@openzeppelin/contracts/access/Ownable.sol
node_modules/@openzeppelin/contracts/utils/ReentrancyGuard.sol
```

---

## 📚 推荐学习资源

### 视频教程
1. **CryptoZombies** - Solidity基础教学
   - https://cryptozombies.io/
   - 免费，互动式学习

2. **Dapp University** - YouTube频道
   - 搜索 "ERC20 Token Tutorial"
   - 搜索 "Uniswap Tutorial"

### 文档
1. **Solidity官方文档**
   - https://docs.soliditylang.org/
   - 重点看：合约、继承、modifier

2. **OpenZeppelin文档**
   - https://docs.openzeppelin.com/
   - 重点看：ERC20、Ownable、安全最佳实践

3. **Uniswap V2文档**
   - https://docs.uniswap.org/contracts/v2/overview
   - 理解 Router 和 Pair

### 实战项目
1. 先写最简单的ERC20
2. 再加税费
3. 再加限制
4. 最后加Uniswap集成

---

## 🔧 调试技巧

### 遇到错误不要慌
```javascript
// 使用 console.log
import "hardhat/console.sol";

function _update(...) internal override {
    console.log("From:", from);
    console.log("To:", to);
    console.log("Amount:", amount);
    // ...
}
```

### 常见错误和解决方法

**错误1：Transfer amount exceeds balance**
```
原因：税费计算后余额不足
解决：检查税费是否从 from 扣除两次
```

**错误2：ERC20: transfer from zero address**
```
原因：在铸造时也收税了
解决：检查 _update 中的零地址判断
```

**错误3：Gas费太高**
```
原因：每次转账都执行了复杂逻辑
解决：添加判断，只在必要时执行
```

---

## 🎓 学习心态建议

### 1. 接受"不懂"是正常的
- 第一次看不懂是正常的
- 需要看3-5遍才能理解
- 写代码比看代码更重要

### 2. 从错误中学习
```
第一次：代码报错 → 看错误信息 → 搜索 → 修复
第二次：又报错 → 对比上次 → 理解原因 → 修复
第三次：不报错了 → 明白了！
```

### 3. 建立自己的代码库
```
my-snippets/
├── simple-erc20.sol      # 最简单的ERC20
├── with-tax.sol          # 加了税费的
├── with-limits.sol       # 加了限制的
└── complete.sol          # 完整版本
```

每次写完一个功能，保存为模板，下次直接用。

---

## 🚀 7天后你应该能做什么

- [ ] 独立写出一个ERC20代币
- [ ] 理解并实现代币税机制
- [ ] 理解并实现交易限制
- [ ] 理解Uniswap的基本集成
- [ ] 写出基本的测试用例
- [ ] 阅读和理解他人的代币合约
- [ ] 修改和定制现有合约

---

## 💪 给自己的鼓励

**记住：**
1. 所有大神都是从零开始的
2. 不懂就问，不要怕显得笨
3. 每天进步一点点就够了
4. 实践 > 理论
5. 犯错是学习的最好方式

**最重要的：**
> 不要试图一次性理解所有代码
> 从最简单的开始
> 每天只学一个新概念
> 7天后回头看，你会惊讶于自己的进步！

---

## 📞 学习中遇到问题？

### 推荐提问的地方
1. **Stack Overflow** - 英文
2. **以太坊中文社区** - 中文
3. **GitHub Issues** - 看别人的问题
4. **Discord/Telegram** - 实时讨论

### 如何提好问题
```
❌ 不好的问题：
"代码报错了怎么办？"

✅ 好的问题：
"我在实现税费功能时，遇到 'Transfer amount exceeds balance' 错误。
我的代码是这样的：[贴代码]
我尝试了：[说明尝试过什么]
但还是不行，请问可能是什么原因？"
```

---

## 🎉 开始你的学习之旅吧！

现在就开始**第1天**的学习：

```bash
# 1. 创建新项目
mkdir my-first-meme-token
cd my-first-meme-token

# 2. 初始化
npm init -y
npm install --save-dev hardhat

# 3. 开始Hardhat项目
npx hardhat init
```

**祝你学习顺利！记住：慢慢来，比较快！** 🚀

---

*这份学习路线图基于实际教学经验编写*  
*预计学习时间：每天2-5小时，共7天*  
*完成后你将具备开发Meme代币合约的能力*
