# 快速开始指南 - SHIB风格Meme代币

## 🎯 5分钟快速体验

### 步骤1：安装依赖（已完成✅）

项目依赖已经安装完成，可以直接使用！

### 步骤2：编译合约

```bash
npx hardhat compile
```

预期输出：
```
Compiled 10 Solidity files successfully
```

### 步骤3：运行测试

```bash
npx hardhat test
```

预期结果：
```
✔ 35 passing (628ms)
```

所有测试通过表示合约功能正常！

### 步骤4：查看测试覆盖

```bash
npx hardhat test --verbose
```

可以看到详细的测试场景。

---

## 📚 深入学习

### 查看理论知识

```bash
# 阅读理论分析文档
code docs/理论知识分析.md
```

**内容包括：**
- 代币税机制详解（3000字）
- 流动性池原理（2500字）
- 交易限制策略（2000字）

### 理解合约代码

```bash
# 查看主合约
code contracts/ShibaMemeToken.sol
```

**核心功能：**
- 代币税：买入5%，卖出10%
- 自动流动性注入
- 单笔限额、最大持有量、冷却期
- 黑名单、启动保护

### 学习测试用例

```bash
# 查看测试文件
code test/ShibaMemeToken.test.js
```

**测试覆盖：**
- 35个测试用例
- 100%功能覆盖
- 边界条件验证

---

## 🚀 本地部署体验

### 方式1：启动本地Hardhat网络

**终端1 - 启动节点：**
```bash
npx hardhat node
```

保持这个终端运行。

**终端2 - 部署合约：**
```bash
npx hardhat run scripts/deploy.js --network localhost
```

部署成功后会显示合约地址和配置信息。

### 方式2：使用Hardhat控制台交互

```bash
npx hardhat console --network localhost
```

在控制台中：
```javascript
// 获取账户
const [owner, user1] = await ethers.getSigners();

// 部署合约
const Token = await ethers.getContractFactory("ShibaMemeToken");
const token = await Token.deploy(
    "Test Meme",
    "TMEME",
    ethers.parseEther("1000000000000"),
    "0x7a250d5630B4cF539739dF2C5dAcb4c659F2488D", // Router地址
    owner.address,
    owner.address
);

// 查看信息
await token.name();
await token.symbol();
await token.totalSupply();

// 启用交易
await token.enableTrading();

// 转账测试
await token.transfer(user1.address, ethers.parseEther("1000"));
```

---

## 📖 查看完整文档

### README - 完整操作手册

```bash
code README.md
```

**包含内容：**
- 项目概述
- 核心功能详解
- 部署指南（本地/测试网/主网）
- 完整操作手册
- 安全考虑
- 常见问题FAQ
- 800+行详细文档

### 部署脚本说明

```bash
# 查看部署脚本
code scripts/deploy.js

# 查看添加流动性脚本
code scripts/addLiquidity.js

# 查看交互示例
code scripts/interact.js
```

### 作业完成总结

```bash
code 作业完成总结.md
```

**包含：**
- 作业要求对照
- 项目结构说明
- 功能实现细节
- 测试验证报告
- 代码统计
- 学习价值分析

---

## 🎓 学习路径建议

### 初学者路线

1. **理解理论** → 阅读 `docs/理论知识分析.md`
2. **查看代码** → 逐行阅读 `ShibaMemeToken.sol`
3. **运行测试** → `npx hardhat test` 查看各功能
4. **本地部署** → 启动本地网络并部署
5. **交互测试** → 使用console进行交互

### 进阶开发者路线

1. **分析架构** → 理解合约继承和模块化设计
2. **研究测试** → 学习如何编写全面的测试
3. **Gas优化** → 分析Gas消耗并优化
4. **安全审计** → 查找潜在安全问题
5. **功能扩展** → 添加新功能（如DAO治理）

---

## 🔧 常用命令速查

```bash
# 编译
npx hardhat compile

# 测试
npx hardhat test

# 部署（本地）
npx hardhat node
npx hardhat run scripts/deploy.js --network localhost

# 清理
npx hardhat clean

# 控制台
npx hardhat console

# 查看账户
npx hardhat accounts

# Gas报告
REPORT_GAS=true npx hardhat test
```

---

## 💡 关键文件速查

| 文件 | 内容 | 行数 |
|------|------|------|
| `contracts/ShibaMemeToken.sol` | 主合约实现 | 635 |
| `contracts/IShibaMeme.sol` | 接口定义 | 183 |
| `test/ShibaMemeToken.test.js` | 测试用例 | 490+ |
| `scripts/deploy.js` | 部署脚本 | 191 |
| `docs/理论知识分析.md` | 理论文档 | 8000+ 字 |
| `README.md` | 完整指南 | 806 |
| `作业完成总结.md` | 项目总结 | 619 |

---

## ⚡ 核心功能速览

### 代币税配置

```javascript
// 查询当前税率
const [buyTax, sellTax] = await token.getTaxRates();

// 修改税率（仅Owner）
await token.setTaxRates(300, 800); // 3%, 8%

// 查询税费分配
const [liq, mark, dev, burn] = await token.getTaxDistribution();
```

### 交易限制管理

```javascript
// 查询当前限制
const [maxTx, maxWallet, cooldown] = await token.getLimits();

// 修改限制（仅Owner）
await token.setLimits(
    ethers.parseEther("5000000000"),  // maxTx
    ethers.parseEther("20000000000")  // maxWallet
);

// 禁用限制
await token.setLimitsEnabled(false);
```

### 黑名单管理

```javascript
// 添加黑名单
await token.setBlacklist("0x...", true);

// 批量设置
await token.setBlacklistBatch(["0x...", "0x..."], true);

// 查询状态
await token.isBlacklisted("0x...");
```

---

## 📝 下一步行动

### 如果你想学习

✅ 已完成所有准备工作，开始学习吧！  
建议从理论文档开始，然后阅读合约代码。

### 如果你想部署测试

1. 配置 `.env` 文件
2. 获取Sepolia测试网ETH
3. 运行 `npx hardhat run scripts/deploy.js --network sepolia`

### 如果你想上线主网

⚠️ **请务必先做：**
1. 进行专业安全审计
2. 在测试网充分测试
3. 准备好营销计划
4. 建立社区支持
5. 确保流动性充足
6. 锁定LP代币

---

## 🎉 恭喜！

你现在拥有了一个功能完整的Meme代币项目！

**项目特点：**
- ✅ 生产级代码质量
- ✅ 完整的测试覆盖
- ✅ 详细的文档说明
- ✅ 开箱即用

**开始你的DeFi之旅吧！** 🚀🌙

---

## 📞 需要帮助？

- 📖 查看 [README.md](README.md) 了解详细操作
- 📚 阅读 [理论知识分析](docs/理论知识分析.md) 理解原理
- 📊 查看 [作业完成总结](作业完成总结.md) 了解项目全貌
- 🧪 运行测试用例学习各功能使用方法

**祝学习/开发顺利！** ⭐
